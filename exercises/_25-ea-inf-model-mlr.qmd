**2.** (a) Positive, moderate $(r = 0.525),$ roughly linear, with one clear outlier (England 6--1 Wales, residual $+2.42$). (b) Predictor: `n_shots` (open-play shots in the match); outcome: `total_xg`. (c) Answers vary; key: predict match chance quality from quick counts. (d) **L**inearity: met (no curve in the residuals); **I**ndependence: reasonable (31 distinct matches); **N**ormality: strained — the England match creates a long right tail; **E**qual variability: questionable — scatter widens at higher predicted values. Conditions are mostly met, with N and E strained by one match.

**4.** (a) Most correlated: `matches` and `sleepnight` $(r = -0.491);$ then `assessment` and `sleepnight` $(r = 0.0398);$ least: `assessment` and `matches` $(r = -0.0351).$ (b) No and no — p-values $0.799$ (`matches`) and $0.773$ (`sleepnight`); neither is a discernible predictor alone. (c) No — p-values $0.898$ and $0.852;$ neither is discernible given the other variable in the model.

**6.** (a) Age 0, not a former professional, 0 years in recruitment — not a plausible staff member. (b) No — all three p-values $(0.876,$ $0.494,$ $0.656)$ are far above 0.05; no discernible association between NDQ-10 score and any staff feature.

**8.** (a) Most correlated: `n_shots` and `total_xg` $(r = 0.699);$ then `goals` and `total_xg` $(r = 0.691);$ least: `goals` and `n_shots` $(r = 0.403).$ (b) Yes and yes — alone, `n_shots` has p-value $2.6 \times 10^{-6}$ and `total_xg` has p-value $2.3 \times 10^{-19};$ each is discernible on its own. (c) No — `total_xg` remains discernible (p-value $\approx 4.6 \times 10^{-15}$) but `n_shots` is not (p-value $= 0.082$), its coefficient flipping from $+0.116$ alone to $-0.045;$ its p-value now tests whether shot count adds anything *given the accumulated chance quality is already in the model* — a different hypothesis from the single-predictor model's, and with these collinear predictors $(r = 0.699)$ the answer is no.

**10.** (a) Observed $\approx 7$ open-play goals; predicted $\approx 3.$ (b) The point is drawn in fold 1's color (held out), so folds 2 and 3 built its prediction model. (c) 7 coefficients for the 6-predictor model (intercept plus six slopes); 3 coefficients for the 2-predictor model. (d) No — both plots run from about $-3.8$ to $+4.1$ with very similar spread; the four extra predictors barely changed the prediction errors.

**12.** (a) Neither — the two residual clouds span nearly identical ranges, so the spread gives no reason to prefer either model. (b) The 2-predictor model: CV SSE $128.4 < 143.2.$ (c) Answers vary; key: overfitting — extra coefficients chase training-fold noise.
